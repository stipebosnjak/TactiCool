Put raw Mixamo animations here to rename the corresponding animation clips.
Run window/ MixamoManager/ click rename

A history file will be created to preserve processed files list.
Renaming process completely resets the FBX importer settings.

Needed as original Mixamo animations are all named the same way : mixamo.com  ^\_(**)_/^